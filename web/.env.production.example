# 本番ビルド時の API ベースURL。Cloud Run のサービスURLを入れる。
# このファイルを .env.production にコピーして値を設定してから `npm run build`。
# （フロントは Cloudflare Workers、API は Cloud Run の別オリジンになるため必須）
VITE_API_BASE=https://<your-service>-<hash>-an.a.run.app
