DATABASE_URL=
PISTON_URL=
PORT=
DEFAULT_USER_ID=
JWT_SECRET=
# development | production （production では JWT_SECRET 必須）
APP_ENV=
# 同一オリジンでフロントを配信する場合のビルド済みディレクトリ。
# ローカル開発は Vite が配信するため空でよい（本番=Docker は /app/web）。
STATIC_DIR=

# PostgreSQL コンテナ初期化用 (docker-compose)
DB_NAME=
DB_USER=
DB_PASSWORD=
